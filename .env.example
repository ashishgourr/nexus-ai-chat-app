# Copy this file to .env (project root for run_with_env.sh) and set your keys.
# Do not commit .env if it contains real keys.
#
# Keys used by the app:
#   GEMINI_API_KEY - Google Gemini API key (https://aistudio.google.com/app/apikey).
#                    Optional: if empty or missing, the app uses mock AI.

GEMINI_API_KEY=your_gemini_api_key_here
